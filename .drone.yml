---
kind: pipeline
name: ci-amd64

platform:
  os: linux
  arch: amd64

steps:
- name: dependencies
  image: gcc
  commands:
  - apt-get -qq update
  - apt -y install build-essential meson ninja-build python3-pip python3-setuptools
- name: install
  image: gcc
  commands:
  - mkdir builddir
  - cd builddir
  - python3 meson .. --prefix=/usr/local/
  - ninja